{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {{ form_start(siteForm) }}

        {{ form_widget(siteForm) }}
        <button>Save</button>

        {{ form_end(siteForm) }}
    </div>
{% endblock %}

{% block title %}
    {{ parent() }} | site creation
{% endblock %}